virtualenv:
  python: python3
  pip:
    requirements:
      - requirements.txt
        
packager:
  target: py-lambda-package.zip
  followlinks: true
  includes:
    - project/**
    - LICENSE
    - static/**
    - templates/**/*.html
  excludes:
    - static/**/*.tmp
